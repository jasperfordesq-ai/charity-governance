{
	admin off
	auto_https off
	persist_config off
}

:8080 {
	encode zstd gzip
	header {
		-Server
		X-Robots-Tag "noindex, nofollow, noarchive"
	}

	import /etc/caddy/active-upstreams.caddy

	log {
		output stdout
		format json
	}
}
